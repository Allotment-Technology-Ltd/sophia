# `@sophia/graph-reasoning-extensions`

Sophia-only graph helpers that depend on `@restormel/contracts` (compare, lineage, projection, etc.).

**Restormel Graph Contract v0** (`GraphData`, layout, trace, workspace) lives in the published package **`@restormel/graph-core`** — not here.

**Publishing / rename to `@restormel/graph-reasoning-extensions`:** see [PUBLISHING.md](./PUBLISHING.md).

node_modules/
yarn.lock
build/
.svelte-kit/
dist/
.venv/
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
*.log
npm-debug.log*
surreal.log
surreal.db
temp/
tmp/
data/backups/*/
data/backups/
# Surreal CLI exports (`surreal export` → *.surql); may contain production graph data
backups/
data/reports/*.md
data/reports/*.json
data/reports/
# Ingest checkpoints, rewinds, batch summaries (local only — entire tree)
data/ingested/**
memory/
.cursor/

# Maintainer-only documentation pack (full Restormel, operations, archive, etc.).
# See docs/LOCAL_DOCS.md. Not present on public clones; keep your checkout in sync privately.
docs/local/

# Local design documents — do not commit work-in-progress design files
docs/design/MASTER-IMPLEMENTATION-GUIDE.md

# Operator batch manifests / inventories (local only)
data/relabel-inventory.csv
data/relabel-inventory.json
data/relabel-neon-*.json
data/sep-reingest-sources.txt
data/sep-reingest-sources.json
data/sep-reingest-sources-urls.txt

# Repomix outputs (large generated packs — do not commit; keep *.config.json tracked)
repomix-output.xml
repomix-output*.xml
repomix-output*.md
repomix-output*.txt
repomix-output*.json
repomix-knowledge-base.xml
repomix-knowledge-base*.xml
repomix-knowledge-base*.md

# Local scratch (often contains SQL/passwords — never commit)
history.txt
Q*:
docs/stoa_game.zip
data/thinker-graph/unmatched-authors.latest.json

# Environment & secrets — never commit real keys
# Local service account JSON (pnpm dev → scripts/dev.mjs discovers secrets/*.json)
secrets/*
!secrets/.gitkeep

.env
.env.local
.env*.local
.env.paddle.generated
.env.paddle.generated.*

# .env.example is intentionally tracked (contains placeholder values only)
.env.paddle.generated
.env.paddle.generated.production

# Source texts and ingested data (copyrighted material — do not publish)
# Local fetch cache (raw HTML by URL hash; optional; see FETCH_SOURCE_CACHE)
data/cache/

data/sources/*.txt
data/sources/*.meta.json
# SEP URL catalog: regenerate with `pnpm sep:catalog` — file is tracked so Docker/runtime can read it (sepEntryBatchPick.ts).
data/thinker-graph/philosophers.json

# Database files
*.db

# Local migration artifacts
data/migration/

# Phase 1 / Phase 2 training exports — large adapter weights and archives (local only)
data/phase1-training-export/**/*.tar.zst
data/phase1-training-export/**/*.tar.gz
data/phase1-training-export/ft-*-adapter/
data/phase1-training-export/adapters/
# Merged full HF weights (multi-GB; repackage for Fireworks / vLLM locally only)
data/phase1-training-export/merged-bfl6-hf/
# Large Together chat repacks (regenerate via ops scripts from `*.jsonl`)
data/phase1-training-export/*.together.jsonl
data/phase1-training-export/*.together.token-stats.json
# Local eval runs, golden shards, checkpoints, job receipts (ignore new files; older copies may stay tracked)
data/phase1-training-export/eval*.json
data/phase1-training-export/eval*.ndjson
data/phase1-training-export/*.ckpt.ndjson
data/phase1-training-export/fireworks-sft-job-submitted.json
# Rewind / scratch backups anywhere under data/
data/**/*.bak

# Private migration docs/scripts (not for public repo)
docs/GCP-ORG-MIGRATION.md
docs/MIGRATION-IMPLEMENTATION-SUMMARY.md
docs/MIGRATION-QUICKSTART.md
docs/ORG-POLICY-TROUBLESHOOTING.md
scripts/check-org-policies.sh
scripts/migration-db-backup.sh
scripts/modify-org-policies.sh
scripts/revert-org-policies.sh
scripts/setup-wif.sh
scripts/transfer-project.sh
scripts/verify-migration.sh
.vibe/
